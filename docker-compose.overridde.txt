
services:
  backend:
    volumes:
      - ./backend:/app

  frontend:
    build:
      context: ./frontend
   
      target: build
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    command: npm run dev  